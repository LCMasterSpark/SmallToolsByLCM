<!-- 文件作用：定义屏幕指针生成工具的内嵌配置界面。 -->
<UserControl x:Class="小工具集合.Views.Generation.ScreenPointerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="Segoe UI, Microsoft YaHei UI">
    <UserControl.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#252526"/>
        <SolidColorBrush x:Key="PanelAltBrush" Color="#2D2D30"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#3F3F46"/>
        <SolidColorBrush x:Key="TextBrush" Color="#F1F1F1"/>
        <SolidColorBrush x:Key="MutedBrush" Color="#969696"/>
        <SolidColorBrush x:Key="SubtleBrush" Color="#C8C8C8"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#68217A"/>

        <Style x:Key="SectionBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
        </Style>

        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource MutedBrush}"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="ValueTextBoxStyle" TargetType="TextBox">
            <Setter Property="Width" Value="64"/>
            <Setter Property="Height" Value="26"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>

        <Style x:Key="PresetColorButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="26"/>
            <Setter Property="Margin" Value="0,0,7,0"/>
            <Setter Property="BorderBrush" Value="#66000000"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#1E1E1E">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="18,14">
            <DockPanel>
                <StackPanel>
                    <TextBlock Text="SCREEN POINTER" Foreground="{StaticResource TextBrush}" FontSize="24" FontWeight="SemiBold"/>
                    <TextBlock Text="生成一枚点击穿透的屏幕准星，专治找不到屏幕中心。" Margin="0,4,0,0" Foreground="{StaticResource MutedBrush}" FontSize="13"/>
                </StackPanel>
                <ToggleButton x:Name="EnabledToggle"
                              DockPanel.Dock="Right"
                              Width="128"
                              Height="34"
                              Content="显示准星"
                              VerticalAlignment="Center"
                              Checked="SettingsControlChanged"
                              Unchecked="SettingsControlChanged"/>
            </DockPanel>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid Margin="18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.05*"/>
                    <ColumnDefinition Width="14"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <Border Style="{StaticResource SectionBorderStyle}">
                        <StackPanel>
                            <Border Height="2" Background="{StaticResource AccentBrush}" Margin="0,0,0,14"/>
                            <TextBlock Text="准星来源" Style="{StaticResource SectionTitleStyle}"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1.1*"/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="内置样式" Style="{StaticResource LabelStyle}"/>
                                    <ComboBox x:Name="StyleComboBox" Height="32" SelectionChanged="StyleComboBox_SelectionChanged"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2" VerticalAlignment="Bottom">
                                    <Button Height="32" Content="上传图片准星" Click="UploadImageButton_Click"/>
                                    <TextBlock x:Name="ImagePathTextBlock" Margin="0,8,0,0" Foreground="{StaticResource MutedBrush}" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource SectionBorderStyle}">
                        <StackPanel>
                            <Border Height="2" Background="{StaticResource AccentBrush}" Margin="0,0,0,14"/>
                            <TextBlock Text="准星参数" Style="{StaticResource SectionTitleStyle}"/>
                            <Grid>
                                <TextBlock x:Name="OpacityLabel" Foreground="{StaticResource SubtleBrush}" VerticalAlignment="Center"/>
                                <TextBox x:Name="OpacityValueTextBox" Style="{StaticResource ValueTextBoxStyle}" LostFocus="SliderValueTextBox_Commit" KeyDown="SliderValueTextBox_KeyDown"/>
                            </Grid>
                            <Slider x:Name="OpacitySlider" Minimum="0.1" Maximum="1" TickFrequency="0.05" ValueChanged="SettingsControlChanged"/>

                            <CheckBox x:Name="OutlineEnabledCheckBox" Content="启用边框" Foreground="{StaticResource TextBrush}" Margin="0,10,0,0" Checked="SettingsControlChanged" Unchecked="SettingsControlChanged"/>
                            <Grid Margin="0,12,0,0">
                                <TextBlock x:Name="OutlineThicknessLabel" Foreground="{StaticResource SubtleBrush}" VerticalAlignment="Center"/>
                                <TextBox x:Name="OutlineThicknessValueTextBox" Style="{StaticResource ValueTextBoxStyle}" LostFocus="SliderValueTextBox_Commit" KeyDown="SliderValueTextBox_KeyDown"/>
                            </Grid>
                            <Slider x:Name="OutlineThicknessSlider" Minimum="2" Maximum="12" TickFrequency="2" IsSnapToTickEnabled="True" ValueChanged="SettingsControlChanged"/>

                            <Grid x:Name="PrimaryParamHeader" Margin="0,12,0,0">
                                <TextBlock x:Name="PrimaryParamLabel" Foreground="{StaticResource SubtleBrush}" VerticalAlignment="Center"/>
                                <TextBox x:Name="PrimaryParamValueTextBox" Style="{StaticResource ValueTextBoxStyle}" LostFocus="SliderValueTextBox_Commit" KeyDown="SliderValueTextBox_KeyDown"/>
                            </Grid>
                            <Slider x:Name="PrimaryParamSlider" ValueChanged="SettingsControlChanged"/>

                            <Grid Margin="0,12,0,0">
                                <TextBlock x:Name="SecondaryParamLabel" Foreground="{StaticResource SubtleBrush}" VerticalAlignment="Center"/>
                                <TextBox x:Name="SecondaryParamValueTextBox" Style="{StaticResource ValueTextBoxStyle}" LostFocus="SliderValueTextBox_Commit" KeyDown="SliderValueTextBox_KeyDown"/>
                            </Grid>
                            <Slider x:Name="SecondaryParamSlider" ValueChanged="SettingsControlChanged"/>

                            <Grid Margin="0,12,0,0">
                                <TextBlock x:Name="ThicknessLabel" Foreground="{StaticResource SubtleBrush}" VerticalAlignment="Center"/>
                                <TextBox x:Name="ThicknessValueTextBox" Style="{StaticResource ValueTextBoxStyle}" LostFocus="SliderValueTextBox_Commit" KeyDown="SliderValueTextBox_KeyDown"/>
                            </Grid>
                            <Slider x:Name="ThicknessSlider" ValueChanged="SettingsControlChanged"/>

                            <Grid Margin="0,12,0,0">
                                <TextBlock x:Name="GapLabel" Foreground="{StaticResource SubtleBrush}" VerticalAlignment="Center"/>
                                <TextBox x:Name="GapValueTextBox" Style="{StaticResource ValueTextBoxStyle}" LostFocus="SliderValueTextBox_Commit" KeyDown="SliderValueTextBox_KeyDown"/>
                            </Grid>
                            <Slider x:Name="GapSlider" ValueChanged="SettingsControlChanged"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <Border Style="{StaticResource SectionBorderStyle}">
                        <StackPanel>
                            <Border Height="2" Background="{StaticResource AccentBrush}" Margin="0,0,0,14"/>
                            <TextBlock Text="目标屏幕" Style="{StaticResource SectionTitleStyle}"/>
                            <TextBlock Text="显示器" Style="{StaticResource LabelStyle}"/>
                            <ComboBox x:Name="DisplayComboBox" Height="32" SelectionChanged="SettingsControlChanged"/>
                            <CheckBox x:Name="LockCenterCheckBox"
                                      Content="锁定屏幕中心"
                                      Foreground="{StaticResource TextBrush}"
                                      Margin="0,12,0,0"
                                      Checked="LockCenterCheckBox_Changed"
                                      Unchecked="LockCenterCheckBox_Changed"/>
                            <Button Height="30" Margin="0,12,0,0" Content="刷新显示器" Click="RefreshDisplaysButton_Click"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource SectionBorderStyle}">
                        <StackPanel>
                            <Border Height="2" Background="{StaticResource AccentBrush}" Margin="0,0,0,14"/>
                            <TextBlock Text="颜色" Style="{StaticResource SectionTitleStyle}"/>
                            <TextBlock Text="准星颜色" Style="{StaticResource LabelStyle}"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="96"/>
                                </Grid.ColumnDefinitions>
                                <Border x:Name="ColorPreviewBorder" Grid.Column="0" Width="32" Height="32" Margin="0,0,8,0" BorderBrush="#66000000" BorderThickness="1" Background="#FF00E5FF"/>
                                <TextBox x:Name="ColorTextBox" Grid.Column="1" Height="32" TextChanged="SettingsControlChanged"/>
                                <Button Grid.Column="2" Height="32" Margin="8,0,0,0" Content="选择" Click="ChooseColorButton_Click"/>
                            </Grid>

                            <TextBlock Text="边框颜色" Style="{StaticResource LabelStyle}" Margin="0,14,0,6"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="96"/>
                                </Grid.ColumnDefinitions>
                                <Border x:Name="OutlineColorPreviewBorder" Grid.Column="0" Width="32" Height="32" Margin="0,0,8,0" BorderBrush="#66000000" BorderThickness="1" Background="#FF000000"/>
                                <TextBox x:Name="OutlineColorTextBox" Grid.Column="1" Height="32" TextChanged="SettingsControlChanged"/>
                                <Button Grid.Column="2" Height="32" Margin="8,0,0,0" Content="选择" Click="ChooseOutlineColorButton_Click"/>
                            </Grid>

                            <UniformGrid Columns="7" Margin="0,12,0,0">
                                <Button Style="{StaticResource PresetColorButtonStyle}" Tag="#FF00E5FF" Background="#FF00E5FF" Click="PresetColorButton_Click"/>
                                <Button Style="{StaticResource PresetColorButtonStyle}" Tag="#FF00FF66" Background="#FF00FF66" Click="PresetColorButton_Click"/>
                                <Button Style="{StaticResource PresetColorButtonStyle}" Tag="#FFFF3030" Background="#FFFF3030" Click="PresetColorButton_Click"/>
                                <Button Style="{StaticResource PresetColorButtonStyle}" Tag="#FFFFFFFF" Background="#FFFFFFFF" Click="PresetColorButton_Click"/>
                                <Button Style="{StaticResource PresetColorButtonStyle}" Tag="#FFFFFF00" Background="#FFFFFF00" Click="PresetColorButton_Click"/>
                                <Button Style="{StaticResource PresetColorButtonStyle}" Tag="#FFFF4DFF" Background="#FFFF4DFF" Click="PresetColorButton_Click"/>
                                <Button Style="{StaticResource PresetColorButtonStyle}" Tag="#FF000000" Background="#FF000000" Click="PresetColorButton_Click"/>
                            </UniformGrid>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource SectionBorderStyle}">
                        <StackPanel>
                            <Border Height="2" Background="{StaticResource AccentBrush}" Margin="0,0,0,14"/>
                            <TextBlock Text="位置" Style="{StaticResource SectionTitleStyle}"/>
                            <Grid>
                                <TextBlock x:Name="RotationLabel" Foreground="{StaticResource SubtleBrush}" VerticalAlignment="Center"/>
                                <TextBox x:Name="RotationValueTextBox" Style="{StaticResource ValueTextBoxStyle}" LostFocus="SliderValueTextBox_Commit" KeyDown="SliderValueTextBox_KeyDown"/>
                            </Grid>
                            <Slider x:Name="RotationSlider" Minimum="0" Maximum="359" TickFrequency="1" ValueChanged="SettingsControlChanged"/>

                            <Grid Margin="0,12,0,0">
                                <TextBlock x:Name="OffsetXLabel" Foreground="{StaticResource SubtleBrush}" VerticalAlignment="Center"/>
                                <TextBox x:Name="OffsetXValueTextBox" Style="{StaticResource ValueTextBoxStyle}" LostFocus="SliderValueTextBox_Commit" KeyDown="SliderValueTextBox_KeyDown"/>
                            </Grid>
                            <Slider x:Name="OffsetXSlider" Minimum="-500" Maximum="500" TickFrequency="1" ValueChanged="SettingsControlChanged"/>

                            <Grid Margin="0,12,0,0">
                                <TextBlock x:Name="OffsetYLabel" Foreground="{StaticResource SubtleBrush}" VerticalAlignment="Center"/>
                                <TextBox x:Name="OffsetYValueTextBox" Style="{StaticResource ValueTextBoxStyle}" LostFocus="SliderValueTextBox_Commit" KeyDown="SliderValueTextBox_KeyDown"/>
                            </Grid>
                            <Slider x:Name="OffsetYSlider" Minimum="-500" Maximum="500" TickFrequency="1" ValueChanged="SettingsControlChanged"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </ScrollViewer>

        <Border Grid.Row="2" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="18,10">
            <TextBlock x:Name="StatusText" Text="准星覆盖层只负责显示，不接管鼠标。" Foreground="{StaticResource MutedBrush}"/>
        </Border>
    </Grid>
</UserControl>
