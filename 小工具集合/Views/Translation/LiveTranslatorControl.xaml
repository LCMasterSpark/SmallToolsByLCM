<!-- 文件作用：实时翻译互动面板，负责启动 OCR 自动化、选择捕获区域和管理悬浮字幕窗。 -->
<UserControl x:Class="小工具集合.Views.Translation.LiveTranslatorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="Segoe UI, Microsoft YaHei UI">
    <Grid Background="#1E1E1E">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="#252526" BorderBrush="#3F3F46" BorderThickness="0,0,0,1" Padding="16,14">
            <DockPanel>
                <Button x:Name="OverlayModeButton"
                        DockPanel.Dock="Right"
                        Width="120"
                        Height="32"
                        Content="切到编辑模式"
                        Click="OverlayModeButton_Click"/>
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="LIVE TRANSLATOR" Foreground="#F1F1F1" FontSize="24" FontWeight="SemiBold"/>
                        <TextBlock x:Name="SmallStatusText" Text="预留 Hook，不注入进程" Foreground="#C8C8C8" Margin="14,8,0,0"/>
                    </StackPanel>
                    <TextBlock Text="悬浮窗 + Windows OCR + 自动去重。借鉴实时翻译体验，但不内嵌 GPL 代码。"
                               Foreground="#C8C8C8"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1" Margin="18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="翻译设置" Foreground="#F1F1F1" FontSize="16" FontWeight="SemiBold"/>
                <TextBlock Text="引擎" Foreground="#C8C8C8" Margin="0,14,0,6"/>
                <ComboBox x:Name="ProviderBox" Height="30"/>

                <Grid Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="源语言" Foreground="#C8C8C8" Margin="0,0,0,6"/>
                        <ComboBox x:Name="SourceLanguageBox" Height="30"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="目标语言" Foreground="#C8C8C8" Margin="0,0,0,6"/>
                        <ComboBox x:Name="TargetLanguageBox" Height="30"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="输入来源" Foreground="#C8C8C8" Margin="0,14,0,6"/>
                <ComboBox x:Name="InputModeBox" Height="30">
                    <ComboBoxItem Content="剪贴板文本"/>
                    <ComboBoxItem Content="全屏 OCR"/>
                    <ComboBoxItem Content="选择区域 OCR"/>
                    <ComboBoxItem Content="指定窗口 OCR"/>
                    <ComboBoxItem Content="前台窗口 OCR"/>
                </ComboBox>

                <TextBlock Text="目标窗口" Foreground="#C8C8C8" Margin="0,14,0,6"/>
                <ComboBox x:Name="TargetWindowBox"
                          Height="30"
                          DisplayMemberPath="Title"
                          SelectionChanged="TargetWindowBox_SelectionChanged"/>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <Button Width="96" Height="30" Content="刷新窗口" Click="RefreshWindowsButton_Click"/>
                    <Button Width="96" Height="30" Content="锁定所选" Margin="8,0,0,0" Click="LockSelectedWindowButton_Click"/>
                </StackPanel>

                <TextBlock Text="悬浮窗位置" Foreground="#C8C8C8" Margin="0,14,0,6"/>
                <ComboBox x:Name="OverlayPlacementBox" Height="30" SelectionChanged="OverlayPlacementBox_SelectionChanged">
                    <ComboBoxItem Content="固定位置"/>
                    <ComboBoxItem Content="跟随目标窗口"/>
                </ComboBox>

                <TextBlock Text="刷新间隔（毫秒）" Foreground="#C8C8C8" Margin="0,14,0,6"/>
                <TextBox x:Name="IntervalBox" Height="30"/>

                <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                    <Button x:Name="StartButton" Width="86" Height="32" Content="开始" Click="StartButton_Click"/>
                    <Button x:Name="StopButton" Width="86" Height="32" Content="停止" Margin="8,0,0,0" Click="StopButton_Click"/>
                    <Button Width="110" Height="32" Content="选择区域" Margin="8,0,0,0" Click="SelectRegionButton_Click"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <Button Width="128" Height="32" Content="显示悬浮窗" Click="ShowOverlayButton_Click"/>
                    <Button Width="128" Height="32" Content="关闭悬浮窗" Margin="8,0,0,0" Click="CloseOverlayButton_Click"/>
                </StackPanel>
            </StackPanel>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="12"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border BorderBrush="#3F3F46" BorderThickness="1" Background="#252526">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Background="#2D2D30" BorderBrush="#3F3F46" BorderThickness="0,0,0,1" Padding="10,0">
                            <TextBlock Text="识别文本" Foreground="#F1F1F1" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        </Border>
                        <TextBox x:Name="RecognizedTextBox"
                                 Grid.Row="1"
                                 IsReadOnly="True"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"
                                 BorderThickness="0"
                                 Background="#1E1E1E"
                                 Foreground="#F1F1F1"
                                 Padding="10"/>
                    </Grid>
                </Border>

                <Border Grid.Row="2" BorderBrush="#3F3F46" BorderThickness="1" Background="#252526">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Background="#2D2D30" BorderBrush="#3F3F46" BorderThickness="0,0,0,1" Padding="10,0">
                            <TextBlock Text="翻译结果" Foreground="#F1F1F1" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        </Border>
                        <TextBox x:Name="TranslatedTextBox"
                                 Grid.Row="1"
                                 IsReadOnly="True"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"
                                 BorderThickness="0"
                                 Background="#1E1E1E"
                                 Foreground="#F1F1F1"
                                 Padding="10"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <Border Grid.Row="2" Background="#252526" BorderBrush="#3F3F46" BorderThickness="0,1,0,0" Padding="16,8">
            <TextBlock x:Name="FooterText"
                       Text="状态：待机。进程 Hook 已预留但未启用。"
                       Foreground="#C8C8C8"/>
        </Border>
    </Grid>
</UserControl>
