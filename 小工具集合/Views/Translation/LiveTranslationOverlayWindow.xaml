<!-- 文件作用：实时翻译的置顶悬浮字幕窗，支持点击穿透和半透明显示。 -->
<Window x:Class="小工具集合.Views.Translation.LiveTranslationOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="实时翻译"
        Width="520"
        Height="150"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="True"
        Topmost="True"
        FontFamily="Segoe UI, Microsoft YaHei UI">
    <Border Background="#E51E1E1E"
            BorderBrush="#68217A"
            BorderThickness="1"
            CornerRadius="6"
            Padding="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <DockPanel MouseLeftButtonDown="OverlayHeader_MouseLeftButtonDown">
                <Button DockPanel.Dock="Right"
                        Width="24"
                        Height="22"
                        Margin="8,0,0,0"
                        Padding="0"
                        Content="×"
                        ToolTip="关闭悬浮窗"
                        Click="CloseButton_Click"/>
                <TextBlock Text="LCM 实时翻译"
                           Foreground="#F1F1F1"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"/>
                <TextBlock x:Name="StatusText"
                           DockPanel.Dock="Right"
                           Text="待机"
                           Foreground="#C8C8C8"
                           FontSize="12"
                           VerticalAlignment="Center"/>
            </DockPanel>
            <TextBlock x:Name="TranslationText"
                       Grid.Row="1"
                       Text="等待识别..."
                       Foreground="White"
                       FontSize="18"
                       TextWrapping="Wrap"
                       Margin="0,8,0,0"/>
        </Grid>
    </Border>
</Window>
