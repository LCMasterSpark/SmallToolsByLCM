<mah:MetroWindow x:Class="小工具集合.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:models="clr-namespace:小工具集合.Models"
                 Title="小工具集合"
                 Width="{Binding WindowWidth, Mode=TwoWay}"
                 Height="{Binding WindowHeight, Mode=TwoWay}"
                 MinWidth="1080"
                 MinHeight="760"
                 GlowBrush="#0078D4"
                 ResizeMode="CanResize"
                 WindowStartupLocation="CenterScreen"
                 FontFamily="Segoe UI, Microsoft YaHei UI"
                 mc:Ignorable="d">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <SolidColorBrush x:Key="PageBackgroundBrush" Color="#F6F9FC"/>
        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SidebarBackgroundBrush" Color="#EFF5FA"/>
        <SolidColorBrush x:Key="LineBrush" Color="#DCE6F0"/>
        <SolidColorBrush x:Key="SubtleTextBrush" Color="#5E6B78"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#0078D4"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#106EBE"/>

        <Style TargetType="TextBox">
            <Setter Property="BorderBrush" Value="#CBD7E3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#1F2933"/>
            <Setter Property="SelectionBrush" Value="#99C9F2"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="BorderBrush" Value="#CBD7E3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#1F2933"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#1F2933"/>
            <Setter Property="BorderBrush" Value="#CBD7E3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="MinHeight" Value="44"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E4F1FB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource PageBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="264"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource SidebarBackgroundBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Margin="22,22,20,18">
                    <TextBlock Text="小工具集合" FontSize="24" FontWeight="SemiBold" Foreground="#111827"/>
                    <TextBlock Text="按大类进入，在右侧选择具体工具。" Margin="0,8,0,0" Foreground="{StaticResource SubtleTextBrush}" TextWrapping="Wrap"/>
                </StackPanel>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Groups}" Margin="10,0,10,0">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type models:ToolGroup}">
                                <Button Style="{StaticResource NavButtonStyle}" Click="GroupButton_Click" Tag="{Binding}" Margin="0,2">
                                    <DockPanel LastChildFill="True">
                                        <TextBlock DockPanel.Dock="Left" FontFamily="Segoe MDL2 Assets" Text="{Binding Icon}" FontSize="16" Foreground="{StaticResource AccentBrush}" Width="30" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding Name}" FontSize="14.5" Foreground="#1F2933" VerticalAlignment="Center"/>
                                    </DockPanel>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <Border Grid.Row="2" Margin="18" Padding="12" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" Background="#F8FBFE">
                    <TextBlock Text="不会保存输入、密钥或历史记录。" Foreground="{StaticResource SubtleTextBrush}" FontSize="12.5" TextWrapping="Wrap"/>
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Margin="26,24,26,18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" LastChildFill="True">
                <StackPanel>
                    <TextBlock Text="{Binding SelectedGroup.Name}" FontSize="30" FontWeight="SemiBold" Foreground="#111827"/>
                    <TextBlock Text="{Binding SelectedTool.Description}" Margin="0,8,0,0" Foreground="{StaticResource SubtleTextBrush}" FontSize="14" TextWrapping="Wrap"/>
                </StackPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Top">
                    <Button Content="清空" Width="84" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0" Command="{Binding ClearCommand}"/>
                    <Button Content="{Binding PauseButtonText}" Width="84" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0" Command="{Binding PauseCommand}" Visibility="{Binding IsPausableTool, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <Button Content="执行" Width="104" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding ExecuteCommand}"/>
                </StackPanel>
            </DockPanel>

            <Border Grid.Row="1" Background="{StaticResource PanelBackgroundBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" Padding="16" Margin="0,20,0,18">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="250"/>
                            <ColumnDefinition Width="26"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="250"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="类型" VerticalAlignment="Center" Foreground="#374151" Margin="0,0,10,0"/>
                        <ComboBox Grid.Column="1" Height="34" ItemsSource="{Binding AvailableTools}" SelectedItem="{Binding SelectedTool}" DisplayMemberPath="Name"/>

                        <TextBlock Grid.Column="3" Text="操作" VerticalAlignment="Center" Foreground="#374151" Margin="0,0,10,0"/>
                        <ComboBox Grid.Column="4" Height="34" ItemsSource="{Binding SelectedTool.Operations}" SelectedItem="{Binding SelectedOperation}" DisplayMemberPath="Name"/>
                    </Grid>

                    <Grid Grid.Row="1" Margin="0,16,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Border BorderBrush="#EDF2F7" BorderThickness="0,1,0,0"/>
                        <ScrollViewer Grid.Row="1" MaxHeight="270" Margin="0,14,0,0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <WrapPanel x:Name="ParametersPanel" Orientation="Horizontal" VerticalAlignment="Top"/>
                        </ScrollViewer>
                    </Grid>
                </Grid>
            </Border>

            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Visibility="{Binding HasInput, Converter={StaticResource BoolToVisibilityConverter}}" Background="{StaticResource PanelBackgroundBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" Padding="14">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <DockPanel Margin="0,0,0,8">
                            <TextBlock Text="输入" FontSize="15" FontWeight="SemiBold" Foreground="#111827"/>
                            <TextBlock DockPanel.Dock="Right" Text="{Binding SelectedTool.InputWatermark}" Foreground="{StaticResource SubtleTextBrush}" TextWrapping="Wrap"/>
                        </DockPanel>
                        <TextBox Grid.Row="1"
                                 Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="True"
                                 AcceptsTab="True"
                                 TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 HorizontalScrollBarVisibility="Auto"
                                 FontFamily="Consolas"
                                 FontSize="14"
                                 Height="96"
                                 Padding="12"/>
                    </Grid>
                </Border>

                <Border Grid.Row="1" Height="14" Visibility="{Binding HasInput, Converter={StaticResource BoolToVisibilityConverter}}"/>

                <Border Grid.Row="2" Background="{StaticResource PanelBackgroundBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" Padding="14">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <DockPanel Margin="0,0,0,8">
                            <TextBlock Text="输出" FontSize="15" FontWeight="SemiBold" Foreground="#111827"/>
                            <Button DockPanel.Dock="Right" Content="复制" Width="76" Height="28" Style="{StaticResource SecondaryButtonStyle}" Click="CopyButton_Click"/>
                        </DockPanel>
                        <TextBox Grid.Row="1"
                                 Text="{Binding OutputText}"
                                 IsReadOnly="True"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 HorizontalScrollBarVisibility="Auto"
                                 FontFamily="Consolas"
                                 FontSize="14"
                                 Padding="12"
                                 Background="#FBFCFE"/>
                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="3" Margin="0,14,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding StatusText}" Foreground="{StaticResource SubtleTextBrush}" TextWrapping="Wrap"/>
                <Border Grid.Column="1" Visibility="{Binding HasWarning, Converter={StaticResource BoolToVisibilityConverter}}" Background="#FFF8E1" BorderBrush="#F1C232" BorderThickness="1" Padding="10,5">
                    <TextBlock Text="{Binding WarningText}" Foreground="#6B4B00"/>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</mah:MetroWindow>
